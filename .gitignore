# Build / deploy output (генерируется из исходников)
public/

# Cloudflare wrangler кэш (служебное, не публиковать)
.wrangler/

# Служебные временные файлы
_commitmsg.txt

# Python артефакты
__pycache__/
*.pyc

# Сгенерированный QR (пересоздаётся из make_qr.py)
qr_artsmile.png

# OS
Thumbs.db
.DS_Store

# Локальные dev-настройки / внутренние материалы по продажам (не для публикации)
.claude/
outreach/
